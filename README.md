# Connect4-3D

To start, go to current directory and use:

```
python -m venv .venv
.venv\Scripts\activate
pip install requrements.txt
```

This installs all required python packages to a viritual enviorment.